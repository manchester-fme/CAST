name: Build SMT-RAT

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 * * * *'  # Run every hour

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      
      - name: Restore solver state cache
        uses: actions/cache@v4
        id: cache-restore
        with:
          path: .cache
          key: solver-smtrat-latest
          restore-keys: |
            solver-smtrat-
      
      - name: Check for upstream changes
        id: check-upstream
        run: |
          ./src/check-upstream.sh smtrat https://github.com/smtrat/smtrat.git
          echo "build_needed=$(cat .build_status | grep build_needed | cut -d'=' -f2)" >> $GITHUB_OUTPUT
          echo "sha=$(cat .build_status | grep sha | cut -d'=' -f2)" >> $GITHUB_OUTPUT
      
      - name: Save solver state cache
        if: steps.check-upstream.outputs.build_needed == 'true'
        uses: actions/cache@v4
        with:
          path: .cache
          key: solver-smtrat-${{ steps.check-upstream.outputs.sha }}
      
      - name: Build SMT-RAT
        if: steps.check-upstream.outputs.build_needed == 'true'
        run: ./src/smtrat/build.sh
      
      - name: Skip build (up to date)
        if: steps.check-upstream.outputs.build_needed == 'false'
        run: echo "✅ SMT-RAT is up to date - skipping build"
